// windres resolves ICON paths against its working directory, rather than the
// directory holding this .rc file. The Makefile deliberately invokes it from
// the repository root, so this path stays rooted there for local and CI builds.
1 ICON "cmd/sercon-gui/assets/sercon-gui.ico"
